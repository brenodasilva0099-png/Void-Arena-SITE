(async function () {
  const teamTable = document.getElementById('teamRankingTable');
  const playerTable = document.getElementById('playerRankingTable');
  const statusEl = document.getElementById('rankingStatus');
  const eventFilter = document.getElementById('rankingEventFilter');
  const esc = (value) => VoidArena.escapeHtml(value ?? '');
  let teamsCache = [];
  const num = (v) => { const n = Number(v || 0); return Number.isFinite(n) ? n : 0; };
  const pct = (w, j) => j ? Math.round((w / j) * 1000) / 10 : 0;
  function logo(team = {}, cls = '') { return team.logo ? `<span class="va-rank-logo ${cls}"><img src="${esc(team.logo)}" alt="${esc(team.name || 'Time')}" /></span>` : `<span class="va-rank-logo ${cls}">${esc((team.tag || team.name || 'T').slice(0, 2).toUpperCase())}</span>`; }
  function socialLabel(key) { return window.VoidArenaSocial?.label?.(key) || ({ discord: 'Discord', steam: 'Steam', xbox: 'Xbox', tiktok: 'TikTok', youtube: 'YouTube', twitter: 'Twitter/X', spotify: 'Spotify', riot: 'Riot ID', ea: 'EA ID', psn: 'PSN', instagram: 'Instagram', website: 'Site' }[String(key).toLowerCase()] || key); }
  function socialIcon(key) { return window.VoidArenaSocial?.iconHtml?.(key) || '🔗'; }
  function socialHref(key, value = '') { const raw = String(value || '').trim(); if (!raw) return ''; if (/^https?:\/\//i.test(raw)) return raw; if (/^discord\.gg\//i.test(raw)) return `https://${raw}`; if (key === 'tiktok') return `https://www.tiktok.com/@${raw.replace(/^@/, '')}`; if (key === 'instagram') return `https://instagram.com/${raw.replace(/^@/, '')}`; if (key === 'twitter') return `https://x.com/${raw.replace(/^@/, '')}`; if (key === 'steam' && /^\d{16,20}$/.test(raw)) return `https://steamcommunity.com/profiles/${raw}`; return ''; }
  function connectionCards(socials = {}) { const entries = Object.entries(socials || {}).filter(([, v]) => String(v || '').trim()); if (!entries.length) return '<p class="va-muted">Nenhuma conexão pública cadastrada.</p>'; return `<div class="va-connections-grid va-social-card-grid">${entries.map(([key, value]) => { const raw = String(value || '').trim(); const href = socialHref(key, raw); const valueLine = href ? '' : `<small>${esc(raw)}</small>`; const body = `<span class="va-social-card-icon">${socialIcon(key)}</span><span class="va-social-card-body"><strong>${socialLabel(key)}</strong>${valueLine}</span><span class="va-social-card-arrow">↗</span>`; return href ? `<a class="va-social-card" href="${esc(href)}" target="_blank" rel="noreferrer">${body}</a>` : `<div class="va-social-card">${body}</div>`; }).join('')}</div>`; }
  function normalizeResult(result = {}) { const match = result.match || {}; const a = match.teamA || result.teamA || {}; const b = match.teamB || result.teamB || {}; const games = Array.isArray(result.games) ? result.games : []; let ga = num(result.goalsA ?? result.totalGoalsA); let gb = num(result.goalsB ?? result.totalGoalsB); if (!ga && !gb && games.length) games.forEach((g) => { if (g.status && g.status !== 'validated') return; ga += num(g.finalScoreA ?? g.scoreA); gb += num(g.finalScoreB ?? g.scoreB); }); if (!ga && !gb) { ga = num(result.finalScoreA); gb = num(result.finalScoreB); } return { teamA: a, teamB: b, goalsA: ga, goalsB: gb, winnerTeamId: String(result.winnerTeamId || ''), eventId: String(result.eventId || match.eventId || result.tournamentEventId || '').trim(), status: String(result.status || '').toLowerCase() }; }
  function baseTeam(team = {}) { return { id: String(team.id || ''), raw: team, name: team.name || team.tag || 'Time', tag: team.tag || '', played: 0, wins: 0, draws: 0, losses: 0, goals: 0, assists: 0, passes: 0, defenses: 0, mvp: 0, interceptions: 0, goalsAgainst: 0, cleanSheets: 0, vap: 0 }; }
  function computeTeams(teams = [], results = [], selectedEventId = '') { const map = new Map(); teams.forEach((t) => map.set(String(t.id), baseTeam(t))); const ensure = (team = {}) => { const id = String(team.id || ''); if (!id) return null; if (!map.has(id)) map.set(id, baseTeam(team)); return map.get(id); }; results.map(normalizeResult).filter((r) => !selectedEventId || r.eventId === selectedEventId || !r.eventId).forEach((r) => { if (r.status !== 'validated' && !r.winnerTeamId) return; const a = ensure(r.teamA); const b = ensure(r.teamB); if (!a || !b) return; const draw = !r.winnerTeamId && r.goalsA === r.goalsB; a.played += 1; b.played += 1; a.goals += r.goalsA; b.goals += r.goalsB; a.goalsAgainst += r.goalsB; b.goalsAgainst += r.goalsA; if (r.goalsB === 0 && r.goalsA > 0) { a.cleanSheets += 1; a.defenses += 1; } if (r.goalsA === 0 && r.goalsB > 0) { b.cleanSheets += 1; b.defenses += 1; } if (draw) { a.draws += 1; b.draws += 1; } else if (r.winnerTeamId === a.id) { a.wins += 1; b.losses += 1; a.mvp += 1; } else if (r.winnerTeamId === b.id) { b.wins += 1; a.losses += 1; b.mvp += 1; } }); map.forEach((r) => { r.assists = Math.floor(r.goals * 0.6); r.passes = r.played ? (r.played * 12) + (r.goals * 3) : 0; r.interceptions = r.played ? (r.played * 4) + r.cleanSheets : 0; r.winRate = pct(r.wins, r.played); r.goalDifference = r.goals - r.goalsAgainst; r.vap = Math.round(((r.wins * 3) + r.draws + (r.goals * 0.5) + r.cleanSheets + (r.played ? 2 : 0)) * 10) / 10; }); return Array.from(map.values()).sort((a, b) => b.vap - a.vap || b.winRate - a.winRate || b.wins - a.wins || b.goalDifference - a.goalDifference || b.goals - a.goals || a.name.localeCompare(b.name)); }
  function playersFromTeams(teamRows = []) { const rows = []; teamRows.forEach((team) => { const titulares = Array.isArray(team.raw.playerDetails) && team.raw.playerDetails.length ? team.raw.playerDetails : (Array.isArray(team.raw.players) ? team.raw.players.map((name, i) => ({ name, discordId: team.raw.playerAccounts?.players?.[i] || '' })) : []); const reservas = Array.isArray(team.raw.reserveDetails) && team.raw.reserveDetails.length ? team.raw.reserveDetails : (Array.isArray(team.raw.reserves) ? team.raw.reserves.map((name, i) => ({ name, discordId: team.raw.playerAccounts?.reserves?.[i] || '' })) : []); [...titulares.map((p) => [p, 'Titular']), ...reservas.map((p) => [p, 'Reserva'])].forEach(([p, role], index) => { const name = String(p?.name || p || '').trim(); if (!name) return; const share = index < titulares.length ? 1 : 0.45; const total = Math.max(1, titulares.length + reservas.length); rows.push({ id: p?.id || '', discordId: p?.discordId || '', name, team: team.name, teamId: team.id, role, passes: Math.round(team.passes * share / total), goals: Math.round(team.goals * share / Math.max(1, titulares.length)), assists: Math.round(team.assists * share / Math.max(1, titulares.length)), defenses: role === 'Reserva' ? 0 : Math.round(team.defenses * share / Math.max(1, titulares.length)), mvp: index === 0 ? team.mvp : 0, interceptions: Math.round(team.interceptions * share / total), points: Math.round((team.vap * share / total) * 10) / 10 }); }); }); return rows.sort((a, b) => b.points - a.points || b.goals - a.goals || a.name.localeCompare(b.name)); }
  function teamById(id) { return teamsCache.find((t) => String(t.id) === String(id)); }
  function playerProfileCard(user, team = null) { const p = user.profile || {}; const name = p.username || user.name || 'Jogador'; const banner = p.banner || p.discordBanner || ''; const socials = { discord: user.discordId ? `<@${user.discordId}>` : '', ...(user.socials || {}) }; const teamGhost = team?.logo ? `<img class="va-public-team-ghost" src="${esc(team.logo)}" alt="" />` : ''; return `<div class="va-modal-card va-public-profile-card"><button class="va-modal-close va-floating-close" data-user-close type="button">×</button><div class="va-public-banner" style="${banner ? `background-image:linear-gradient(180deg,rgba(0,0,0,.05),rgba(0,0,0,.72)),url('${esc(banner)}')` : ''}">${!banner && team?.logo ? `<img class="va-user-team-watermark" src="${esc(team.logo)}" alt="" />` : ''}</div><div class="va-public-head">${teamGhost}<div class="va-profile-page-avatar va-public-avatar">${user.avatar ? `<img src="${esc(user.avatar)}" alt="Avatar" />` : esc(name.slice(0,1).toUpperCase())}</div><div><p class="va-eyebrow">Perfil público do jogador</p><h2>${esc(name)}</h2><p class="va-muted">${esc([p.country, p.region || p.competitiveRegion, p.primaryPosition].filter(Boolean).join(' • ') || user.discordId || '')}</p></div></div><div class="va-public-section"><h3>Conexões</h3>${connectionCards(socials)}</div>${p.bio ? `<div class="va-public-section"><h3>Bio</h3><p class="va-muted">${esc(p.bio)}</p></div>` : ''}</div>`; }
  async function openUser(userId, teamId = '') { if (!userId) return; const data = await VoidArena.request(`/api/users/${encodeURIComponent(userId)}/public`); const overlay = getOverlay('rankingUserOverlay', '[data-user-close]'); overlay.innerHTML = playerProfileCard(data.user, teamById(teamId)); overlay.hidden = false; }
  function getOverlay(id, closeSelector) { let overlay = document.getElementById(id); if (!overlay) { overlay = document.createElement('div'); overlay.id = id; overlay.className = 'va-modal-shell'; document.body.appendChild(overlay); overlay.addEventListener('click', (e) => { if (e.target === overlay || e.target.closest(closeSelector)) overlay.hidden = true; }); } return overlay; }
  async function openTeam(teamId) { const data = await VoidArena.request(`/api/teams/${encodeURIComponent(teamId)}/public`); const team = data.team; const overlay = getOverlay('rankingPublicOverlay', '[data-rank-close]'); const players = [...(team.playerDetails || []), ...(team.reserveDetails || [])]; overlay.innerHTML = `<div class="va-modal-card va-public-profile-card va-team-public-card"><button class="va-modal-close va-floating-close" data-rank-close type="button">×</button><div class="va-public-banner va-team-public-banner">${team.logo ? `<img src="${esc(team.logo)}" alt="" />` : ''}</div><div class="va-public-head"><div class="va-public-team-logo">${team.logo ? `<img src="${esc(team.logo)}" alt="Logo ${esc(team.name)}" />` : esc((team.tag || team.name || 'T').slice(0, 2).toUpperCase())}</div><div><p class="va-eyebrow">Perfil público do time</p><h2>${esc(team.name)} ${team.tag ? `<span class="va-muted">(${esc(team.tag)})</span>` : ''}</h2><p class="va-muted">Capitão: <button class="va-link-button va-profile-public-link" data-user-open="${esc(team.ownerUserId || team.captainDiscordId || '')}" data-team-id="${esc(team.id)}">${esc(team.captainName || team.ownerName || 'não definido')}</button></p><div class="va-kpi-row"><span class="va-badge">Titulares ${(team.playerDetails || []).length}</span><span class="va-badge">Reservas ${(team.reserveDetails || []).length}</span><span class="va-badge">${esc(team.tag || 'TIME')}</span></div></div></div><div class="va-public-section"><h3>Conexões</h3>${connectionCards(team.socials)}</div><div class="va-public-section"><h3>Elenco</h3><div class="va-team-roster">${players.map((p) => `<button class="va-player-row as-button" data-user-open="${esc(p.id || p.discordId || '')}" data-team-id="${esc(team.id)}">⚽ ${esc(p.name || 'Jogador')}<span class="va-muted">${esc(p.discordId || '')}</span></button>`).join('') || '<div class="va-player-row">Nenhum jogador detalhado.</div>'}</div></div></div>`; overlay.querySelectorAll('[data-user-open]').forEach((b) => b.addEventListener('click', () => openUser(b.dataset.userOpen, b.dataset.teamId))); overlay.hidden = false; }
  function renderTeams(rows = []) { teamTable.innerHTML = '<thead><tr><th>#</th><th>Time</th><th class="num">VAP</th><th class="num">J</th><th class="num">V</th><th class="num">E</th><th class="num">D</th><th>Win-rate</th><th class="num">Gols</th><th class="num">Assist.</th><th class="num">Defesas</th><th class="num">MVP</th><th class="num">Intercep.</th></tr></thead><tbody>' + (rows.length ? rows.map((r, i) => `<tr data-team-open="${esc(r.id)}"><td class="va-rank-pos">#${i + 1}</td><td><button class="va-rank-entity as-button" type="button">${logo(r.raw, 'large')}<span class="va-rank-name"><strong>${esc(r.name)}</strong><small>${esc(r.tag || 'sem tag')}</small></span></button></td><td class="num"><strong class="va-vap-score">${r.vap}</strong></td><td class="num">${r.played}</td><td class="num">${r.wins}</td><td class="num">${r.draws}</td><td class="num">${r.losses}</td><td><div class="va-winrate"><strong>${r.winRate}%</strong><div class="va-winrate-bar"><span style="width:${Math.min(100, r.winRate)}%"></span></div></div></td><td class="num">${r.goals}</td><td class="num">${r.assists}</td><td class="num">${r.defenses}</td><td class="num">${r.mvp}</td><td class="num">${r.interceptions}</td></tr>`).join('') : '<tr><td colspan="13">Nenhum time cadastrado para ranquear.</td></tr>') + '</tbody>'; teamTable.querySelectorAll('[data-team-open]').forEach((row) => row.addEventListener('click', () => openTeam(row.dataset.teamOpen).catch((e) => { statusEl.textContent = e.message; statusEl.className = 'va-status err'; }))); }
  function renderPlayers(rows = []) { playerTable.innerHTML = '<thead><tr><th>#</th><th>Jogador</th><th>Time</th><th>Função</th><th class="num">Pts</th><th class="num">Passes</th><th class="num">Gols</th><th class="num">Assist.</th><th class="num">Defesas</th><th class="num">MVP</th><th class="num">Intercep.</th></tr></thead><tbody>' + (rows.length ? rows.map((r, i) => `<tr data-user-open="${esc(r.id || r.discordId)}" data-team-id="${esc(r.teamId)}"><td class="va-rank-pos">#${i + 1}</td><td><strong class="va-profile-public-link">${esc(r.name)}</strong></td><td>${esc(r.team)}</td><td><span class="va-badge">${esc(r.role)}</span></td><td class="num"><strong>${r.points}</strong></td><td class="num">${r.passes}</td><td class="num">${r.goals}</td><td class="num">${r.assists}</td><td class="num">${r.defenses}</td><td class="num">${r.mvp}</td><td class="num">${r.interceptions}</td></tr>`).join('') : '<tr><td colspan="11">Nenhum jogador cadastrado nos times ainda.</td></tr>') + '</tbody>'; playerTable.querySelectorAll('[data-user-open]').forEach((row) => row.addEventListener('click', () => openUser(row.dataset.userOpen, row.dataset.teamId).catch((e) => { statusEl.textContent = e.message; statusEl.className = 'va-status err'; }))); }
  function renderEvents(events = []) { const selected = eventFilter.value || ''; eventFilter.innerHTML = '<option value="">Todos os eventos</option>' + events.map((event) => `<option value="${esc(event.id)}">${esc(event.name || event.title || 'Evento')}</option>`).join(''); eventFilter.value = selected; }
  async function load() { statusEl.textContent = 'Carregando rankings...'; statusEl.className = 'va-status'; const [teamsData, eventsData, resultsData] = await Promise.all([VoidArena.request('/api/teams'), VoidArena.request('/api/events'), VoidArena.request('/api/match-results').catch(() => ({ results: [] }))]); const selectedEventId = eventFilter.value || ''; teamsCache = teamsData.teams || []; const events = eventsData.events || []; const results = resultsData.results || resultsData.records || []; renderEvents(events); eventFilter.value = selectedEventId; const teamRows = computeTeams(teamsCache, results, selectedEventId); renderTeams(teamRows); renderPlayers(playersFromTeams(teamRows)); statusEl.textContent = 'Rankings atualizados. Clique em um time ou jogador para abrir o perfil público.'; statusEl.className = 'va-status ok'; }
  eventFilter.addEventListener('change', load); document.getElementById('reloadRankingsBtn')?.addEventListener('click', load); VoidArena.bootLayout('rankings').then(load).catch((error) => { statusEl.textContent = error.message; statusEl.className = 'va-status err'; });
}());
